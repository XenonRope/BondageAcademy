{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "pwa-node",
      "request": "attach",
      "name": "Debug",
      "websocketAddress": "${input:websocketAddress}"
    }
  ],
  "inputs": [
    {
      "id": "websocketAddress",
      "type": "promptString",
      "description": "Enter the WebSocket address"
    }
  ]
}
